# def quick_sort(arr):
#     # Base case: An array of 0 or 1 element is already sorted
#     if len(arr) <= 1:
#         return arr
#     # Step 1: Choose pivot as the first element (low)
#     pivot = arr[0]

#     # Step 2: Divide the remaining elements into two parts
#     left = []
#     right = []

#     # Loop through the array starting from the second element
#     for x in arr[1:]:
#         if x < pivot:
#             left.append(x)
#         else:
#             right.append(x)

#     # Step 3: Recursively sort left and right, then combine with pivot
#     return quick_sort(left) + [pivot] + quick_sort(right)

# n = int(input("Enter size: "))
# arr = list(map(int, input("Enter elements: ").split()))

# if len(arr) != n:
#         print("Error: size mismatch")
# else:
#         result = quick_sort(arr)
#         print("Sorted array:", *result)

# def quicksort(arr):
#     if len(arr)<=1:
#         return arr
#     pivot= arr[0]

#     left=[]
#     right=[]

#     for x in arr[1:]:
#         if x < pivot:
#             left.append(x)
#         else:
#             right.append(x)

#     return quicksort(left) +[pivot] + quicksort(right)

# n=int(input("Enter size:"))
# arr=list(map(int,input("enter elements:").split()))

# if len(arr)!=n:
#     print("size mismatched.")
# else:
#     result=quicksort(arr)
#     print("sorted array:", *result)

def quicksort(arr):
    if len(arr)<=1:
        return arr
    pivot= arr[0]
    left=[]
    right=[]

    for x in arr[1:]:
        if x< pivot:
            left.append(x)
        else:
            right.append(x)

    return quicksort(left) + [pivot]+ quicksort(right)

n=int(input("Enter size:    "))
arr=list(map(int,input("Enter elements:").split()))

if len(arr)!=n:
    print("size mismatched.")
else:
    result= quicksort(arr)
    print("sorted array:", *result)

